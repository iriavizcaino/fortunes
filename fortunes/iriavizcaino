If you want to go to the previous directory use *cd -*
%
For update code use *git fetch* or *git pull*
%
First time you want to edit \opt  *sudo chrowm -R $USER*
%
*git reset HEAD^ --soft* delete the last commit without losing changes
%